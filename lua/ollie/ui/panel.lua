local M = {}

-- local function M.open(content)
--     local buf = vim.api.nvim_create_buf(false, true) -- listed, scratch buffer
--     local lines = vim.split(content, "\n")
--     vim.api.nvim_buf_set_lines(buf, 0, -1, false, lines)

    return M