# ollie.nvim

AI-integrated neovim plugin.

2. selection context

Visual selection support.

3. floating panel stability

Improve:

scrolling
close behavior
markdown 4) router abstraction

Right now commands probably directly call provider.

Eventually:

command
↓
router
↓
provider selection 5) async streaming

Major milestone.
